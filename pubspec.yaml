name: flutter_artist_backend_sandbox_demo
description: "FlutterArtist Backend Sandbox Demo"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.9.0
#resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  flutter_artist_backend_sandbox: ^0.9.2
  dio: ^5.9.2
  msw_dio_interceptor: ^1.0.5
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:

  uses-material-design: true
